version: '3.8'

services:
  backend:
    build: ./Backend
    container_name: springboot_backend
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
